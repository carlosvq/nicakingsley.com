---
interface Props {
  headline: string;
  title: string;
  price: number;
  link: string;
  included: string[];
}

const { headline, title, price, link, included } = Astro.props;
---

<div
  class="mx-auto mt-8 max-w-2xl rounded-3xl ring-1 ring-gray-200 md:mt-12 lg:mx-0 lg:flex lg:max-w-none"
>
  <div class="p-3 md:p-6 lg:flex-auto">
    <div class="text-xl font-bold tracking-tight text-gray-900">
      {title}
    </div>
    <div class="mt-4 text-sm leading-7 text-gray-600">
      <slot />
    </div>

    <div class="mt-6 flex items-center gap-x-4">
      <div class="flex-none text-lg font-semibold leading-6 text-green-900">
        ¿Qué incluye?
      </div>
      <div class="h-px flex-auto bg-gray-100"></div>
    </div>

    <div class="mt-8">
      <ul
        role="list"
        class="grid grid-cols-1 gap-2 text-sm leading-6 text-gray-600 md:grid-cols-2 md:gap-4"
      >
        {
          included.map((item) => (
            <li class="flex gap-x-3">
              <svg
                class="h-6 w-5 flex-none text-green-900"
                viewBox="0 0 20 20"
                fill="currentColor"
                aria-hidden="true"
              >
                <path
                  fill-rule="evenodd"
                  d="M16.704 4.153a.75.75 0 01.143 1.052l-8 10.5a.75.75 0 01-1.127.075l-4.5-4.5a.75.75 0 011.06-1.06l3.894 3.893 7.48-9.817a.75.75 0 011.05-.143z"
                  clip-rule="evenodd"
                />
              </svg>
              {item}
            </li>
          ))
        }
      </ul>
    </div>
  </div>
  <div class="-mt-2 p-2 lg:mt-0 lg:w-full lg:max-w-md lg:flex-shrink-0">
    <div
      class="rounded-2xl bg-gray-50 py-10 text-center ring-1 ring-inset ring-gray-900/5 lg:flex lg:flex-col lg:justify-center lg:py-16 h-full"
    >
      <div class="mx-auto max-w-xs px-8">
        <p class="text-base font-semibold text-gray-600">
          {headline}
        </p>
        <p class="mt-6 flex items-baseline justify-center gap-x-2">
          <span class="text-5xl font-bold tracking-tight text-gray-900">
            ${price}
          </span>
          <span
            class="text-sm font-semibold leading-6 tracking-wide text-gray-600"
            >USD</span
          >
        </p>
        <a
          href={link}
          class="mt-10 block w-full rounded-md bg-green-900 px-3 py-2 text-center text-sm font-semibold !text-white shadow-sm hover:bg-green-950 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-green-900"
          >Iniciar Ahora</a
        >
        <div class="mt-4 text-xs leading-5 text-gray-600">
          Facturas y recibos disponibles para un fácil reembolso por parte de la
          empresa.
        </div>
      </div>
    </div>
  </div>
</div>
